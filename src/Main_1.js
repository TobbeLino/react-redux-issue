// import styled from '@emotion/styled';          // <--- If this (only) is imported here, Main_2 will start working (both <LangProvider> and <GlobalStyles>)
// import { LangProvider } from './LangProvider'; // <--- If this (only) is imported here, <LangProvider> in Main_2 will start working (but not <GlobalStyles>)

export const Main_1 = () => {
    return (
        <div>I'm Main_1</div>
    );
}
